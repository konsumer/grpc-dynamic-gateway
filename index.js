/**
 * generate middleware to handle proto
 * @param  {String} proto Filename of proto-file
 * @param  {String} grpc  HOST:PORT of grpc server
 * @return {Function}     Middleware
 */
module.exports = (proto, grpc) => {
  return (req, res, next) => {
  }
}
